# Frozen configuration

`frozen_analysis_config.json` consolidates the main fixed design and model specifications used by the included scripts.

The executable scripts remain the authoritative implementation. The JSON file is provided to make participant splits, model hyperparameters, CNN seeds and ensembling, feature definitions, and sensitivity-analysis settings easy to inspect without reading the source code.
